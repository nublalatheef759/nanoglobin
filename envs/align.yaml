# Alignment + BAM handling: minimap_align, samtools_sort, samtools_index
name: nanoglobin-align
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - minimap2=2.24
  - samtools=1.16.1
