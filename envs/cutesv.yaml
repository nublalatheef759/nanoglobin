# cuteSV SV calling. conda-forge first for the same dependency-resolution reason.
name: nanoglobin-cutesv
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.10
  - cutesv=1.0.8
  - pysam=0.22.0
  
  