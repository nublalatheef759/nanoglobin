# Clair3 SNV/indel calling — self-contained (own python 3.9 + pysam/whatshap),
# which is what removes the module-vs-conda pysam conflict and the two-shell split.
name: nanoglobin-clair3
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - clair3=1.0.4
